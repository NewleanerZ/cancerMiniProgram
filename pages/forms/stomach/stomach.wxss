/* pages/forms/stomach/stomach.wxss */
/* 基础容器样式 - 与食管问卷保持一致 */
.container {
  padding: 20rpx 30rpx;
  background-color: #f7f9fc;
  min-height: 100vh;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

/* 章节标题样式 - 保持视觉统一性 */
.section-title {
  font-size: 34rpx;
  font-weight: 600;
  color: #2c6ecb;
  margin: 40rpx 0 25rpx;
  padding-left: 15rpx;
  border-left: 6rpx solid #337ab7;
  line-height: 1.4;
}

/* 问题项容器 - 复用卡片式设计 */
.question-item {
  background-color: #fff;
  padding: 35rpx 30rpx;
  margin-bottom: 25rpx;
  border-radius: 16rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.04);
  transition: box-shadow 0.2s ease;
}

.question-item:hover {
  box-shadow: 0 6rpx 16rpx rgba(0, 0, 0, 0.06);
}

/* 问题文本样式 */
.question {
  font-size: 32rpx;
  margin-bottom: 25rpx;
  color: #333;
  line-height: 1.6;
  padding-right: 10rpx;
}

/* 问题补充说明文本 */
.question-sub {
  font-size: 28rpx;
  color: #666;
  margin: -15rpx 0 20rpx 0;
  line-height: 1.5;
  padding-left: 5rpx;
}

/* 子问题样式（饮食偏好下的细分问题） */
.sub-question {
  margin: 25rpx 0 15rpx 30rpx; /* 缩进显示，区分主问题 */
  padding: 10rpx 0;
  border-left: 2rpx solid #e0e6ed;
  padding-left: 20rpx;
}

.sub-question text {
  font-size: 30rpx;
  color: #444;
  margin-right: 15rpx;
}

/* 选项容器 - 保持flex布局一致性 */
.options {
  display: flex;
  flex-direction: column;
  gap: 22rpx;
}

/* 单选框样式 - 复用原有设计 */
.radio {
  display: flex;
  align-items: center;
  font-size: 29rpx;
  color: #555;
  padding: 12rpx 10rpx;
  border-radius: 8rpx;
  transition: background-color 0.2s ease;
  flex-wrap: wrap;
}

.radio:hover {
  background-color: #f5f8ff;
}

.radio radio,
.radio checkbox {
  margin-right: 18rpx;
  transform: scale(0.9); /* 统一单选/复选框大小 */
}

/* 复选框样式 - 与单选框保持视觉统一 */
.checkbox-group {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

/* 输入框样式 - 完全复用食管问卷设计 */
.inline-input,
.small-input,
.normal-input {
  border: 1.5rpx solid #d0d7e3;
  border-radius: 8rpx;
  font-size: 28rpx;
  padding: 12rpx 15rpx;
  transition: all 0.2s ease;
  background-color: #fff;
}

.inline-input:focus,
.small-input:focus,
.normal-input:focus {
  border-color: #337ab7;
  box-shadow: 0 0 0 3rpx rgba(51, 122, 183, 0.1);
  outline: none;
}

.inline-input {
  flex: 1;
  min-width: 300rpx;
  margin-left: 10rpx;
}

.small-input {
  width: 110rpx;
  margin: 0 8rpx;
  text-align: center;
}

.normal-input {
  flex: 1;
  min-height: 70rpx;
}

/* 条件字段容器 - 保持缩进设计 */
.condition-fields {
  margin-top: 20rpx;
  margin-left: 55rpx;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  padding-left: 10rpx;
  border-left: 2rpx dashed #e0e6ed;
}

.field-item {
  display: flex;
  align-items: center;
  font-size: 29rpx;
  color: #555;
  flex-wrap: wrap;
  gap: 10rpx;
}

.field-label {
  margin-right: 5rpx;
  white-space: nowrap;
  color: #444;
  font-size: 29rpx;
}

/* 提交按钮样式 - 完全复用确保交互一致性 */
.submit-btn {
  margin: 50rpx 0 30rpx;
  width: 100%;
  height: 90rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  line-height: 1;
  background: linear-gradient(135deg, #337ab7 0%, #286090 100%);
  color: white;
  font-size: 34rpx;
  font-weight: 500;
  border-radius: 12rpx;
  border: none;
  box-shadow: 0 6rpx 16rpx rgba(51, 122, 183, 0.2);
  transition: all 0.2s ease;
}

.submit-btn:not([disabled]):hover {
  background: linear-gradient(135deg, #2d6bc7 0%, #23548a 100%);
  box-shadow: 0 8rpx 20rpx rgba(51, 122, 183, 0.25);
  transform: translateY(-2rpx);
}

.submit-btn:not([disabled]):active {
  transform: translateY(0);
  box-shadow: 0 4rpx 12rpx rgba(51, 122, 183, 0.2);
}

/* 对话框样式 - 复用确保系统风格统一（若需要弹窗展示结果） */
.dialog-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.5);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 999;
  pointer-events: auto;
}

.dialog-container {
  width: 85%;
  max-width: 600rpx;
  background-color: #fff;
  border-radius: 20rpx;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  box-shadow: 0 15rpx 40rpx rgba(0, 0, 0, 0.15);
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

.dialog-title {
  padding: 30rpx;
  text-align: center;
  border-bottom: 1px solid #f0f0f0;
}

.high-risk {
  color: #e63946;
  font-size: 36rpx;
  font-weight: 600;
}

.low-risk {
  color: #43aa8b;
  font-size: 36rpx;
  font-weight: 600;
}

.suggestion-container {
  flex: 1;
  padding: 30rpx;
  box-sizing: border-box;
}

.suggestion-scroll {
  height: 100%;
  font-size: 28rpx;
  line-height: 1.7;
  color: #333;
  padding: 5rpx;
}

.dialog-buttons {
  display: flex;
  border-top: 1px solid #f0f0f0;
}

.dialog-btn {
  flex: 1;
  height: 100rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
  padding: 0;
  margin: 0;
  border-radius: 0;
  border: none;
  font-size: 32rpx;
  background-color: transparent;
  color: #1677ff;
}

.dialog-btn::after {
  border: none;
}

.dialog-btn:first-child {
  border-right: 1px solid #f0f0f0;
}

.dialog-btn:active {
  background-color: #f5f5f5;
}